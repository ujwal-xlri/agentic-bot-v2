# ── LangChain (only packages used in code) ────────────────────────────────────
langchain-core==1.3.0
langchain-ollama==1.1.0
langchain-chroma==1.1.0
langchain-huggingface==1.2.2

# ── Vector DB ─────────────────────────────────────────────────────────────────
chromadb==0.5.23

# ── Embeddings & Reranker ─────────────────────────────────────────────────────
sentence-transformers==3.3.1

# ── Unstructured API client ───────────────────────────────────────────────────
# PDFs are parsed by the self-hosted unstructured-api sidecar — only requests needed.
requests

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit==1.40.2

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
openpyxl==3.1.5
pymupdf
